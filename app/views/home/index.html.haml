
.jumbotron
  .display-4 
    What do you wany to learn today?

  .my-4 

  .form.inline.mr-2.my-lg-0
    = form_tag(courses_path, method: :get) do
      .input-group
        = text_field_tag :title, params[:title], autocomplete: "off", class: "input form-controlsm-2", placeholder: "Search", :type => "search"
        %span.input.group-append
          %button.btn.btn-primary.mr-2.my-sm-0{:type => "submit"}
            %span.fa.fa-search{"aria-hidden" => "true"}

%hr
%h2
  Popular Courses
.card-columns
  - @popular_courses.each do |course|
    = render "courses/course", course: course

%hr
%h2
  Top rated Courses
.card-columns
  - @top_rated_courses.each do |course|
    = render "courses/course", course: course

%hr
%h2
  Latest Courses
.card-columns
  - @latest_courses.each do |course|
    = render "courses/course", course: course